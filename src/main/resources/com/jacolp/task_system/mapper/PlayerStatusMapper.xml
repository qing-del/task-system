<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.jacolp.task_system.mapper.PlayerStatusMapper">
    <insert id="registerInsert">
        insert into player_status (spirit, body, current_exp, total_exp, level, update_time, user_id, coin)
        values (
        <choose>
            <when test="spirit != 1">
                1
            </when>
            <otherwise>
                #{spirit}
            </otherwise>
        </choose>

        <choose>
            <when test="body != 1">
                , 1
            </when>
            <otherwise>
                , #{body}
            </otherwise>
        </choose>

        <choose>
            <when test="currentExp != 0">
                , 0
            </when>
            <otherwise>
                , #{currentExp}
            </otherwise>
        </choose>

        <choose>
            <when test="totalExp != 10">
                , 10
            </when>
            <otherwise>
                , #{totalExp}
            </otherwise>
        </choose>

        <choose>
            <when test="level != 1">
                , 1
            </when>
            <otherwise>
                , #{level}
            </otherwise>
        </choose>

        , now(), #{userId}
        <choose>
            <when test="coin != 1">
                , 1
            </when>
            <otherwise>
                , #{coin}
            </otherwise>
        </choose>
        )
    </insert>
</mapper>